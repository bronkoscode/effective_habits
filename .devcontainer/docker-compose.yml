version: '3.7'
services:
  # Update this to the name of the service you want to work with in your docker-compose.yml file
  app:
    image: c++
    volumes:
      - /home/bernd/Projects/effective_habits:/workspaces/effective_habits:rw
      